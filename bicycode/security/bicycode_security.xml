<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <!-- note that for now base.group_user has the read right on bicycodes -->
    <record id="group_bicycode_manager" model="res.groups">
        <field name="name">Bicycode Manager</field>
        <field name="users" eval="[(4, ref('base.user_admin'))]" />
    </record>

</odoo>
